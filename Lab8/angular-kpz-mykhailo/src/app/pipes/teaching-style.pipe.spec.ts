import { TeachingStylePipe } from './teaching-style.pipe';

describe('TeachingStylePipe', () => {
  it('create an instance', () => {
    const pipe = new TeachingStylePipe();
    expect(pipe).toBeTruthy();
  });
});
